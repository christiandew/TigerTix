# Port your platform injects; defaults to 8080 if unset
PORT=8080

# CORS allowlist for the frontend
FRONTEND_ORIGIN=http://localhost:3000

# Path to the SQLite file (use a mounted disk/volume in production)
DB_PATH=/var/data/tigertix.sqlite

# Auth + signing
JWT_SECRET=change-me

# LLM access
OPENAI_API_KEY=sk-...
